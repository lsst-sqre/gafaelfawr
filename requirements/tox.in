# tox dependencies.  -*- conf -*-
#
# Add tox and its plugins here. These will be installed in the user's venv for
# local development and by CI when running tox actions.
#
# After editing, update requirements/tox.txt by running:
#     make update-deps

-c main.txt
-c dev.txt

tox
tox-docker
tox-uv

# docker 6.1.3 doesn't work with requests 2.31.0.
requests<2.32.0
